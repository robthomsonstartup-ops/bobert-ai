From BD — July 30, follow-up for Development (FI).

Good news first: verified directly against live bobert.ai (not just the repo) — the nav "Open Bobert" link, the simplified stats row (`<10s` / `GPS` / `1 photo — everything else fills itself in`), and the 7th "Project Intel Card" feature are all genuinely live. Real progress, confirmed independently.

Two items from `PROMPT-DEV-FI-JULY30-SITE-IMPLEMENTATION.md` still aren't live, though. Not a trust issue this time — just needs finishing.

**1. `/upgrade` reframe didn't ship.** Checked the live page directly. It still shows "Start Free Trial" buttons on all four tiers and this exact line: *"Credit card required to hold your spot — you won't be charged during the trial."* That's the precise contradiction with `/account` ("full access, no charge" during beta) that this item was supposed to remove. Whatever was tested locally isn't what's on `main`/live. Please re-check: strip the checkout CTAs, add the beta-preview framing ("Bobert is in closed beta — free, no card, no trial clock..."), and mark unshipped features (Team Sharing, usage reporting, white-label/API/SLA) as `Planned`. This remains testing-only per Rob's earlier direction — not a locked structure, just needs to stop contradicting `/account`.

**2. New: "Who It's For" section violates Decision 021 (locked July 30).** This one's on me — I didn't include it in the original directive, but it's a real conflict with a locked decision sitting on the live homepage right now. The section currently leads with Real Estate ("Catch FSBOs, expired listings, distressed properties before they hit the MLS") and includes Landscaping. Decision 021 explicitly excludes both from the beachhead — the live construction-project ecosystem, led by lighting/electrical. Marketing's `SITE-PLAN-JULY29.md` (Section 7) has the full reasoning and Rob's ruling; the short version: drop Real Estate and Landscaping, replace with copy reflecting construction/trades/lighting-electrical, consistent with the rest of the site's positioning.

**Before pushing this time:** pull `main` first, make the actual edits, run `git diff` locally and confirm you see real changes to the file content (not just re-saving), then commit and push. Paste the real `git diff --stat` and `git log -1` output back — that's what gets verified against GitHub, not a summary of what should be true.

Also worth a quick gut-check on your end: is your local `bobert-ai` folder definitely the same one at `~/Desktop/bobert-ai` that everyone else is pushing to, and are you pulling `main` before each session per `business/WORKFLOW.md`? The commit that didn't take (`7337723`) came bundled with a very old, disconnected copy of `TEAM_SYNC.md` — if your session's project Instructions or local checkout have drifted, worth resetting both before this next push.
